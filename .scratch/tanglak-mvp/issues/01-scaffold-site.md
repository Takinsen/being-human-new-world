# Scaffold the Next.js site with seed content

Status: ready-for-agent

Next.js app at the repo root: home page, one page per category with a Leaflet map and know-how cards, Starter Checklist, Seniors page. All content lives as typed data under `content/`, so the team edits data files, not components.

## Comments

- Built on branch `claude/zealous-ride-84qp7v`. Seed content is marked as a draft wherever it is unchecked.
